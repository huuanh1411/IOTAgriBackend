import 'package:flutter/material.dart';

/// Bang mau chu dao cho theme "AgriSense" - lay cam hung tu nong trai / thuy canh.
class AppColors {
  AppColors._();

  // Xanh la cay - mau thuong hieu chinh
  static const Color primary = Color(0xFF2E7D32);
  static const Color primaryDark = Color(0xFF1B5E20);
  static const Color primaryLight = Color(0xFF60AD5E);
  static const Color primarySurface = Color(0xFFE8F5E9);

  // Xanh nuoc - dai dien cho do am / nuoc trong he thuy canh
  static const Color water = Color(0xFF0288D1);
  static const Color waterSurface = Color(0xFFE1F5FE);

  // Vang dat / nang - dai dien cho nhiet do, canh bao nhe
  static const Color amber = Color(0xFFF9A825);
  static const Color amberSurface = Color(0xFFFFF8E1);

  // Tim - dai dien cho pH
  static const Color violet = Color(0xFF6A1B9A);
  static const Color violetSurface = Color(0xFFF3E5F5);

  // Cam - dai dien cho TDS / dinh duong
  static const Color orange = Color(0xFFEF6C00);
  static const Color orangeSurface = Color(0xFFFFF3E0);

  // Trang thai
  static const Color online = Color(0xFF2E7D32);
  static const Color offline = Color(0xFF9E9E9E);
  static const Color danger = Color(0xFFC62828);
  static const Color dangerSurface = Color(0xFFFFEBEE);

  // Nen / be mat
  static const Color background = Color(0xFFF6FAF6);
  static const Color surface = Color(0xFFFFFFFF);
  static const Color textPrimary = Color(0xFF1B2A1D);
  static const Color textSecondary = Color(0xFF5E6B60);
  static const Color divider = Color(0xFFE1E9E2);
}
